# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the template carries no secrets and documents the safety switches.
!.env.example

# Claude Code — the shared config ships, personal overrides do not
.claude/settings.local.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Working notes, hackathon research, CALL-E doc mirrors, demo films — local only
docs/

# Vendored CALL-E reference repos (cloned separately, each has its own .git)
misc/call-e-resources/awesome-phone-call-agents/
misc/call-e-resources/call-e-integrations/
image.png
CLAUDE.md
.claude/
.impeccable/
